# Mega Ventas

La documentacion principal del proyecto esta en el archivo `README.md` ubicado en la raiz del repositorio.

Este archivo se deja solo como referencia para evitar informacion duplicada o desactualizada dentro de `src`.
